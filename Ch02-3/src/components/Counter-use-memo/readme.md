# 只看這幾個檔案就好 作者放了很多該章節無用檔案

## 三個counter對比

| 工具 | 快取的對象 | 解決的問題 |
|---|---|---|
| `React.memo` | 元件 | props 沒變，跳過元件重渲染 |
| `useCallback` | 函式 | 函式參考不變，配合 `memo` 才有效 |
| `useMemo` | 計算結果 | 避免每次渲染都重跑昂貴的計算 |

**關鍵思考點**：`React.memo` 單獨用，傳進去的函式 props 每次還是新的 → 要配 `useCallback` 才真的有效。